#!/usr/bin/env bash
# kill-chipmunk — stop the notify-chipmunk speech daemon

DAEMON="$HOME/bin/notify-chipmunk"

if pkill -f "$DAEMON$" 2>/dev/null; then
    echo "notify-chipmunk killed."
else
    echo "notify-chipmunk is not currently running."
fi
